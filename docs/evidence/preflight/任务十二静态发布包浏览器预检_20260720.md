# 任务十二：静态发布包浏览器预检

> 发布版本：`20260720-predeploy.1`
>
> 源提交：`24649357b36b78ddb20a99753b739ed4c7720581`
>
> 状态：`predeploy-verified-not-deployed`

## 结论

生成后的静态目录已通过文件完整性、真实 HTTP 加载、桌面布局、390px 布局和 G1 模拟帧回放。页面保持“全屋运行台”定位，没有评分操作界面；本次没有上传到任何托管平台、没有分配域名、没有烧录开发板。

## 发布包证据

| 项目 | 结果 |
| --- | --- |
| 本地目录 | `dist/smartlife-junior-solar-home-v1/`（可再生成，不进 Git） |
| 留档清单 | `task12-static-release-manifest-20260720.json` |
| 运行方式 | `static-https-web-serial` |
| 资源数量 | 7 个：入口、CSS、4 个 JS、`.nojekyll` |
| 清单 SHA-256 | `237ab6fabfdc1244e8f75649d4508d7e169b2e92dd307bb58b0f313b2fd347ed` |
| 云端控制中继 | 无 |
| 自动部署 | 无 |

`tools/verify_static_release.py` 已逐项验证文件存在、大小、SHA-256、版本查询参数及目录无额外文件。页面引用的 `style.css` 和 4 个 JavaScript 文件均带 `?v=20260720-predeploy.1`。

## 浏览器回归

通过本地 HTTP 在 Chrome 中打开实际 `dist` 目录，而不是源码目录。Playwright 两项测试全部通过：

1. 桌面端加载入口、五个带版本号的资源和清单，HTTP 全部返回 `200`；`window.isSecureContext=true`，重放至遥测 `seq=12` 后显示 Sleep 恢复态、风扇 35%、无安全告警和模拟水印。
2. 390px 重放至遥测 `seq=9` 后显示卫生间水浸 P1、风扇 100% 和模拟水印；`body`、`documentElement` 的 `scrollWidth` 均不超过 `390px`。

两个页面均为控制台 `0 error / 0 warning`、请求失败 `0`，正文不含评分操作词。

## 截图完整性

| 文件 | 尺寸 | SHA-256 |
| --- | --- | --- |
| `task12-static-release-desktop-20260720.png` | `1280×2256` | `01794aa5b5c563bf3fdad08b30929c7154c619f66e56ef2a94c83eff112a6be5` |
| `task12-static-release-mobile-390-20260720.png` | `390×5329` | `949022a98ab3d6c505655ca8a10080e46250ae5b04a480811b8ccc091283a0c8` |

## 软件回归

- Python：`36/36` 通过。
- 固件原生：`24/24` 通过。
- N16R8 目标编译：成功，RAM `6.5%`、Flash `5.4%`。
- Dashboard：协议 `9/9`、告警 `6/6`、语音 `10/10`、页面合同和模拟回放通过。
- Playwright：`2/2` 通过。
- npm 离线审计：`0 vulnerabilities`。

## 证据边界

- localhost 是浏览器部署演练，不是公网 HTTPS 上线证明。
- 模拟帧继续带 `source=mock`；截图不能当真板动作证据。
- Web Serial 只连接打开页面这台电脑的 USB 开发板，静态服务器不能远程控制设备。
- 真实域名、TLS、响应头、麦克风权限、USB 串口和真板闭环均须在下一阶段分别验证。
