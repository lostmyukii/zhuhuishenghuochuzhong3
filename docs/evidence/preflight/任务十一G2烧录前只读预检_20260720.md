# 任务十一：G2 烧录前只读预检

> 项目：`smartlife-junior-solar-home-v1`
>
> 记录时间：2026-07-20 00:34（Asia/Shanghai）
>
> 状态：`hardware-preflight-pending`；未烧录、未打开串口监视器

## 结论

固定 GPIO 合同与已编译固件产物均可复核，但当前 macOS 没有枚举到 N16R8 常见 USB 数据串口，六项现场电气检查也尚无实测证据。因此本次只读预检不构成 G2 通过，明确禁止执行上传命令。

用户此前确认“各信号口连接就绪”保留为接线状态说明，但不等同于电压、共地、触发极性或实体动作已经验收。

## 只读检查结果

| 检查项 | 结果 | 证据边界 |
| --- | --- | --- |
| USB 数据串口 | 未发现 `cu.usbserial-*`、`cu.wchusbserial-*`、`cu.SLAB_USBtoUART*` 或 `cu.usbmodem*` | 只说明本次枚举没有候选；蓝牙、EV3 和 debug-console 不是目标串口 |
| 固件产物 | `firmware/.pio/build/n16r8_esp32s3/firmware.bin`，`356192` bytes | 证明已生成二进制，不证明已上传或可在真板运行 |
| 固件 SHA-256 | `3ab9b174cf35ce3143fa0d8fe0eea5885ba07568aa6d284b67cc230a2137a4ca` | 用于之后核对实际上传前的产物一致性 |
| 固定 GPIO | 16 项全部与冻结合同一致 | 包含继电器 `GPIO12`、旋钮 `GPIO17`；只证明源码常量一致 |
| 上传状态 | `uploadAuthorized=false`、`uploadPerformed=false` | 本次没有执行 `pio ... -t upload` |

结构化原始记录：`task11-g2-readonly-preflight-20260720.json`。

## 必须现场完成的六项检查

| 项目 | 当前状态 | 通过证据 |
| --- | --- | --- |
| GPIO17 电压 | 待实测 | 万用表记录最大值 `≤3.3V` |
| MQ2 AO 分压 | 待实测 | 万用表记录进入 GPIO2 前最大值 `≤3.3V` |
| 外部电源共地 | 待实测 | 风扇、舵机、继电器电源与 N16R8 GND 接线照片/通断记录 |
| 继电器空载 | 待实测 | 不接负载，记录上电不误吸合、实际触发极性和状态灯 |
| 舵机脱杆 | 待实测 | 脱开机械连杆，记录方向、安全端点和无堵转 |
| 接线照片 | 待补 | 电源、模块丝印、GPIO 线号清晰可见 |

## 后续真板入口条件

1. 使用具备数据传输能力的 USB 线重新连接，重复只读枚举，得到唯一且可解释的目标串口。
2. 在断电改线的前提下完成上述六项检查，并把实测值和照片放入 `docs/evidence/real-board/`。
3. 上传前再次核对固件 SHA-256；只有用户明确授权后，才可把已确认串口传给 PlatformIO。
4. 烧录后仍需以真实 `hello/telemetry/event/ack`、OLED 和实物动作完成 G3，不能沿用模拟证据代替。
