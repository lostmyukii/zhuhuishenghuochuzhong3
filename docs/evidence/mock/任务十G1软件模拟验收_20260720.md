# 任务十：G1 软件模拟验收

> 日期：2026-07-20
>
> 结论：`mock-passed`
>
> 证据等级：软件、模拟主板与本地 Chromium；不是实体硬件证据

## 1. 确定性原始证据

- 主板输出：`task10-g1-frames-20260720.jsonl`
  - 23 行，全部带 `source=mock`。
  - SHA-256：`50c927c17fc9b8b315d65bb438459cf0f3f66ac17b4dc521a8a4e0d20084c0fb`。
- 外发命令：`task10-g1-commands-20260720.jsonl`
  - 4 行，全部带 `origin=test`。
  - SHA-256：`9f0edebf89eaf60ae5d31b85d2ea49776afc383d91de1db955f5556245049b02`。
- 结构化结论：`task10-g1-summary-20260720.json`
  - 12 个 telemetry 的 `seq` 连续为 `1..12`。
  - 九类检查全部为 `passed=true`。

## 2. 场景矩阵

| 场景 | 可复现结果 | 结论 |
| --- | --- | --- |
| 项目身份 | `smartlife-junior-solar-home-v1`、`source=mock` | 通过 |
| Auto/Sleep | Auto 基线；命令进入 Sleep；A 键事件返回 Auto | 通过 |
| GPIO17 跨阈值 | `T=28℃` 时，`XN=0/YZ=18℃ → 风扇100%`；`XN=4095/YZ=35℃ → 风扇0%` | 通过 |
| 小暑/大寒 | 小暑窗帘 `80%`、补光阈值 `30`；大寒窗帘 `20%`、补光阈值 `45` | 通过 |
| 水浸 P1 | Sleep 下风扇 `100%`、RGB 红、继电器目标关、蜂鸣器报警 | 通过 |
| 三秒恢复 | 水浸输入清除并连续安全三秒后，告警清空，Sleep 风扇恢复 `35%` | 通过 |
| 命令闭环 | 四个命令均有同 ID 成功 ACK，后续 telemetry 带对应 `lastAppliedCommandId` | 通过 |
| 停止遥测 | 3500ms 边界仍新鲜，3501ms 判离线；停止后无新帧 | 通过 |

## 3. Chromium 可见验收

使用本地静态服务打开 `http://127.0.0.1:8767/dashboard/`，通过 `SmartLifeConsoleTest` 重放上述 23 行原始主板输出。

- `seq=1`：Auto、阈值 `27℃`、风扇 `0%`。
- `seq=2`：Sleep、风扇 `35%`、普通窗帘调节暂停。
- `seq=3`：返回 Auto。
- `seq=4`：阈值 `18℃`、风扇 `100%`。
- `seq=5`：阈值 `35℃`、风扇 `0%`。
- `seq=6`：小暑、窗帘 `80%`。
- `seq=7`：大寒、窗帘 `20%`。
- `seq=9`：水浸 P1，页面显示“P1 安全联动正在执行”，风扇 `100%`。
- `seq=12`：告警恢复，Sleep 风扇 `35%`、蜂鸣器安静、RGB 关闭。
- 停止输入并等待 `4200ms`：页面显示“开发板离线”，T/H/风扇清空为 `--`，安全结论变为“等待实时数据”。
- 390px：`innerWidth=390`、页面与 body 的 `scrollWidth=390`。
- Chromium 控制台：0 错误、0 警告。

截图：

- `task10-g1-water-20260720.png`：`1200×2332`，水浸 P1。
- `task10-g1-offline-20260720.png`：`1200×2281`，停止遥测后的离线清空。

## 4. 全量软件闸门

- 完整日志：`task10-g1-test-output-20260720.txt`。
- 结构化退出码与日志哈希：`task10-g1-test-summary-20260720.json`。
- 日志 SHA-256：`8a7ead7db0a0f320c6f428662bf4c985e8b38b48c2ef2e3dcc2c9790b331d638`。

| 检查 | 结果 |
| --- | --- |
| Python 合同、模拟器与 G1 生成器 | `29/29` |
| 固件原生测试 | `24/24` |
| Dashboard 协议 | `9/9` |
| Dashboard 告警 | `6/6` |
| Dashboard 语音 | `10/10` |
| 页面合同与 11 帧旧证据回放 | 通过 |
| N16R8 目标编译 | 成功；RAM `6.5%`，Flash `5.4%` |
| `git diff --check` | 通过 |

## 5. 状态边界

- 当前可标记 `mock-passed`。
- 本次没有选择真实串口、上传固件、读取真板帧、验证真人麦克风或观察实体动作。
- G2 电气预检、G3 真板验收和 G4 彩排仍为 `pending`。
