# 节气智居验收状态

> 当前最高状态：`mock-passed`
>
> 更新时间：2026-07-20

| 闸门 | 状态 | 权威证据 | 尚未证明 |
| --- | --- | --- | --- |
| G0 规格确认 | `design-covered` 已通过 | 主规格、实施计划、固定 GPIO 合同 | 不等于代码或实物通过 |
| G1 软件模拟 | `mock-passed` 已通过 | `docs/evidence/mock/task10-g1-summary-20260720.json`、`task10-g1-test-summary-20260720.json`、任务十浏览器验收记录 | 不等于真板、真实麦克风或实物动作通过 |
| G2 真板烧录前 | `pending` | 尚无万用表、电源共地、MQ2 分压、继电器空载与舵机脱杆记录 | 禁止上传固件 |
| G3 真板验收 | `pending` | 尚无真 `hello/telemetry/event/ack`、OLED 与固定 GPIO 实测证据 | 禁止标记 `real-board-passed` |
| G4 满分就绪 | `pending` | 尚无三次连续单人彩排、断网/重启彩排与全部实物证据 | 禁止标记 `full-score-ready` |

## `mock-passed` 的证据范围

- 项目标识固定为 `smartlife-junior-solar-home-v1`，模拟主板帧全部带 `source=mock`。
- 23 条模拟主板帧、4 条外发命令、12 个连续遥测序号构成 G1 原始证据。
- Auto/Sleep、GPIO17 阈值两端、小暑/大寒、水浸触发与三秒恢复、命令 ACK 对应关系及 `3500/3501ms` 离线边界均通过。
- Chromium 重放同一份原始 JSONL 后，各阶段可见状态与模拟主板一致；停止新帧后清空实时结论。
- 全部 Python、固件原生、目标固件编译和 Dashboard 测试通过。

## 强制边界

- `mock-passed` 不能用于宣称模块已经接线正确、MQ2 分压已实测、继电器触点已动作或风扇/舵机/RGB/蜂鸣器已经产生实体效果。
- 在 G2 人工电气检查完成且用户明确授权前，不上传固件。
- 模拟证据只放在 `docs/evidence/mock/`；真板证据只能放在 `docs/evidence/real-board/`。
