# 节气智居部署上线前检查清单

> 目标：停在“发布包已验证、尚未上传到托管平台”的位置
>
> 网页形态：静态 HTTPS + 当前电脑本地 Web Serial
>
> 当前状态：`predeploy-verified-not-deployed`

## 已完成

- [x] 页面不出现评分、得分、满分或评委操作文案；控制台只呈现全屋运行状态。
- [x] 页面保持 `smartlife-junior-solar-home-v1` 身份锁、3.5 秒离线清空和模拟数据水印。
- [x] 语音只能映射到四句固定口令和 `Auto/Sleep`，不能覆盖 P1 安全动作。
- [x] 构建脚本只收集六个 Dashboard 源文件，拒绝远程脚本、WSS、MQTT、Service Worker 和评分操作词。
- [x] 发布目录为相对资源路径，支持站点根目录或 GitHub Pages 子路径。
- [x] CSS/JS 带发布版本查询参数；`asset-manifest.json` 记录每个文件的大小和 SHA-256。
- [x] 发布包核验器会拒绝缺失、篡改和额外文件。
- [x] 提供 HTTPS 安全响应头与缓存策略模板，但未安装到任何服务器。

## 本地发布包

| 项目 | 当前值 |
| --- | --- |
| 发布版本 | `20260720-predeploy.1` |
| 目录 | `dist/smartlife-junior-solar-home-v1/` |
| 入口 | `index.html` |
| 运行方式 | `static-https-web-serial` |
| 云端设备代理 | 无 |
| 自动部署工作流 | 无 |
| 实际公网地址 | 尚未分配 |

发布包是可再生成文件，`dist/` 不进入 Git。下一次构建前，应保存需要留档的 `asset-manifest.json` 或使用新的发布版本号。

## 上线动作前仍需完成

- [x] 在最后一次 Dashboard/浏览器回归提交后重新生成发布包；`sourceCommit=24649357b36b78ddb20a99753b739ed4c7720581`。
- [x] 用本地 HTTP 服务打开生成后的发布目录，完成桌面和 390px 浏览器回归，控制台 `0 error / 0 warning`。
- [x] 核对页面入口、CSS、4 个 JS 和清单全部由发布目录返回且为 HTTP `200`，不存在源码目录隐式依赖。
- [ ] 选择实际托管平台、域名和 TLS 证书。
- [ ] 在目标 HTTPS 地址核对 Web Serial 可用性、麦克风权限和实际安全响应头。
- [ ] 上线后下载公开文件并与 `asset-manifest.json` 逐项比对。

## 与真板验收的关系

网页具备部署条件不等于实物项目具备满分证据。当前 G2/G3 仍缺少 USB 数据串口、万用表检查、继电器空载、舵机脱杆、真实 `hello/telemetry/event/ack` 和执行器动作。部署不得改变 [docs/验收状态.md](验收状态.md) 中的最高状态 `mock-passed`。

## 明确停止线

本阶段不得执行以下动作：

- 不创建或推送 `gh-pages` 分支；
- 不启用 GitHub Pages、Vercel、Netlify、Cloudflare Pages 或服务器配置；
- 不写入真实域名、证书、服务器地址或凭据；
- 不以公网页面替代真板证据；
- 不烧录开发板。
